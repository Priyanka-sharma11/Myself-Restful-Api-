<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Posts</title>
</head>
<body>
    <h1>MYSELF</h1>
    <% for(post of posts){%>
        <h2><%= post.username%></h2>    
    <div class="card">
        <div class="div">
            <img src="<%=post.img%>" alt="IMAGE">
            <p class="caption"><%=post.caption%></p>
        </div>
        <div class="div">
            <form action="/posts/<%=post.id%>?_method=DELETE" method="post">
                <button>DELETE POST</button>
            </form>
            <button><a href="http://localhost:8080/posts/<%=post.id%>/edit">Edit POST</a></button>
            <button><a href="http://localhost:8080/posts/<%=post.id%>">Show details</a></button>
        </div>
    </div>
    <br><br>
    <% } %>

    <button>
        <a href="http://localhost:8080/posts/new">Create new post</a>
    </button>
</body>
</html>